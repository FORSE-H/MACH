name: Validate Entries

on:
  pull_request:
    paths:
      - 'entries/**'
      - 'data/schema/**'
      - 'data/context/**'
  push:
    branches: [main]
    paths:
      - 'entries/**'

jobs:
  validate:
    name: Validate JSON-LD entries
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
        with:
          egress-policy: audit # tighten to 'block' with allowed-endpoints once baseline is known

      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install -g ajv-cli jsonld

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install requests

      - name: Validate JSON syntax on all entries
        run: |
          echo "Checking JSON syntax..."
          find entries/ -name "*.jsonld" ! -name "_template.jsonld" | while read f; do
            python3 -c "import json; json.load(open('$f'))" && echo "OK: $f" || { echo "FAIL: $f"; exit 1; }
          done

      - name: Check required fields
        run: |
          python3 - <<'EOF'
          import json, os, sys

          REQUIRED = ["identifier", "name", "description", "url",
                      "mach:category", "mach:judgement", "mach:judgementReason",
                      "mach:editorialReviewedAt", "mach:evidence"]

          # license required except for hosted-service catalog entries
          CATALOG_TYPES = {"schema:DataCatalog"}

          VALID_JUDGEMENTS = {"Adopt", "Situational", "Assess", "Caution"}

          errors = []
          for root, dirs, files in os.walk("entries"):
              for fname in files:
                  if not fname.endswith(".jsonld") or fname.startswith("_"):
                      continue
                  path = os.path.join(root, fname)
                  with open(path) as f:
                      data = json.load(f)

                  types = data.get("@type", [])
                  if isinstance(types, str):
                      types = [types]
                  is_catalog = bool(CATALOG_TYPES & set(types))

                  for field in REQUIRED:
                      if field not in data:
                          errors.append(f"{path}: missing required field '{field}'")

                  if not is_catalog and "license" not in data:
                      errors.append(f"{path}: missing required field 'license'")

                  if "mach:judgement" in data:
                      if data["mach:judgement"] not in VALID_JUDGEMENTS:
                          errors.append(f"{path}: invalid judgement '{data['mach:judgement']}'")


                  if "identifier" in data and "logo" in data:
                      logo_path = data["logo"].replace("../../", "")
                      if not os.path.exists(logo_path):
                          print(f"WARN: {path}: logo file not found at {logo_path}")

          if errors:
              for e in errors:
                  print(f"ERROR: {e}")
              sys.exit(1)
          else:
              print(f"All entries valid.")
          EOF

      - name: Check for duplicate identifiers
        run: |
          python3 - <<'EOF'
          import json, os, sys
          from collections import Counter

          ids = []
          for root, dirs, files in os.walk("entries"):
              for fname in files:
                  if not fname.endswith(".jsonld") or fname.startswith("_"):
                      continue
                  path = os.path.join(root, fname)
                  with open(path) as f:
                      data = json.load(f)
                  if "identifier" in data:
                      ids.append(data["identifier"])

          dupes = [id for id, count in Counter(ids).items() if count > 1]
          if dupes:
              print(f"ERROR: Duplicate identifiers found: {dupes}")
              sys.exit(1)
          else:
              print(f"No duplicate identifiers. {len(ids)} entries checked.")
          EOF

      - name: Check primary URLs for dead links
        run: python scripts/check_urls.py --primary-only
